// Comments are made with double slashes

/* Block comments can be made with star slash */

/// Doc comments can be made with three slashes
//! Or two slashes with an exclamation point

/*
 * Big blocks can be commented out on many lines
 * With slashes and stars together
 * Like this!
 */

// the main function is defined with fn main()
fn main() {
    // functions are defined with fn

    // prints with println!("string")
    println!("Hello World!");
    println!("I'm a rustacean!");

    let x = 5 + /* 90 + */ 5;
    println!("Is `x` 10 or 100? x = {}", x);
}